function play() {
	console.log(dummy);
}

const bankAccount = {
	firstName: "John",
	lastName: "Doe",
	watches: ["Rolex", "Casio"],
	balance: 5566,
	add: function (amount) {
		return (this.balance += amount);
	},
};

var alto = {
	age: 14,
	number: 9912134,
};

var name = document.getElementById();

for (var i = 0; i < 2; i++) {
	watches[i];
}
